<%= locals.errorMessage %>
<!-- every local variable defined in the res.
render actually belongs to res.locals and it should be made explicit by setting it to be  
<%= locals.errorMessage %> when passing to the view -->